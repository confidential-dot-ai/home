{{- $attestationServiceURL := printf "http://%s-attestation-service.%s.svc:8400" .Release.Name .Release.Namespace -}}
{{/*
  cds mode points a single --cds-url at the unified CDS, which serves both
  /attest and /ca.
*/}}
{{- $cdsURL := printf "https://%s-cds.%s.svc:8443" .Release.Name .Release.Namespace -}}
{{- $certMode := .Values.certMode -}}
{{- $excludeSourceNamespaces := append .Values.iptablesSync.excludeSourceNamespaces .Release.Namespace | uniq | join "," -}}
{{- /*
The iptables-cleanup preStop sleep fires last in the native-sidecar
reverse-init order; its budget is what terminationGracePeriod leaves after
the main proxy drain. A higher value gets SIGKILL'd mid-cleanup, leaking
iptables chains and ipsets across pod restarts. Fail at template time so
the misconfig is caught before the rollout starts.
*/ -}}
{{- $tgp := include "ratls-mesh.durationSeconds" .Values.terminationGracePeriod | int -}}
{{- $drain := include "ratls-mesh.durationSeconds" .Values.drainTimeout | int -}}
{{- $preStopSleep := int .Values.iptablesCleanup.preStopSleepSeconds -}}
{{- $preStopUpperBound := sub $tgp $drain -}}
{{- if le $tgp $drain }}
{{- fail (printf "terminationGracePeriod (%s) must exceed drainTimeout (%s) to leave a preStop budget" .Values.terminationGracePeriod .Values.drainTimeout) }}
{{- end }}
{{- if lt $preStopSleep 0 }}
{{- fail (printf "iptablesCleanup.preStopSleepSeconds must be ≥ 0, got %d" $preStopSleep) }}
{{- end }}
{{- if gt $preStopSleep $preStopUpperBound }}
{{- fail (printf "iptablesCleanup.preStopSleepSeconds must be ≤ %d (terminationGracePeriod=%s minus drainTimeout=%s; higher values get SIGKILL'd mid-cleanup), got %d" $preStopUpperBound .Values.terminationGracePeriod .Values.drainTimeout $preStopSleep) }}
{{- end }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "ratls-mesh.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ratls-mesh.labels" . | nindent 4 }}
    app.kubernetes.io/component: service-mesh
spec:
  selector:
    matchLabels:
      {{- include "ratls-mesh.selectorLabels" . | nindent 6 }}
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        {{- include "ratls-mesh.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.ports.health | quote }}
        prometheus.io/path: "/metrics"
    spec:
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . }}
      {{- end }}
      # iptables-sync must run in the host network namespace to intercept
      # node-level pod traffic rather than only the mesh pod's own traffic.
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      serviceAccountName: {{ include "ratls-mesh.fullname" . }}
      automountServiceAccountToken: true
      terminationGracePeriodSeconds: {{ $tgp }}
      securityContext:
        {{- if .Values.tpm.enabled }}
        supplementalGroups: {{ .Values.tpm.supplementalGroups | toJson }}
        {{- end }}
        seccompProfile:
          type: RuntimeDefault
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      tolerations:
        - operator: "Exists"
          effect: "NoExecute"
        - operator: "Exists"
          effect: "NoSchedule"
      volumes:
        - name: xtables-lock
          hostPath:
            path: {{ .Values.xtablesLockPath }}
            type: FileOrCreate
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 1Mi
      initContainers:
        # Native sidecar (K8s 1.29+): cleanup runs last on shutdown because
        # sidecars stop in reverse initContainer order.
        - name: iptables-cleanup
          restartPolicy: Always
          image: {{ include "ratls-mesh.image" . }}
          securityContext:
            runAsNonRoot: false
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              add:
                - NET_ADMIN
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: xtables-lock
              mountPath: /run/xtables.lock
          command:
            - /bin/sh
            - -c
            - "trap 'exit 0' TERM; while true; do sleep 3600 & wait $!; done"
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - |
                    /app/c8s ratls-mesh iptables-cleanup
                    sleep {{ .Values.iptablesCleanup.preStopSleepSeconds }}
          resources:
            {{- toYaml .Values.iptablesCleanup.resources | nindent 12 }}
        - name: iptables-sync
          restartPolicy: Always
          image: {{ include "ratls-mesh.image" . }}
          securityContext:
            runAsNonRoot: false
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              add:
                - NET_ADMIN
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: xtables-lock
              mountPath: /run/xtables.lock
            - name: tmp
              mountPath: /tmp
          command:
            - /app/c8s
            - ratls-mesh
            - iptables-sync
            - --outbound-port
            - {{ .Values.ports.outbound | quote }}
            - --uid
            - {{ .Values.uid | quote }}
            - --exclude-uids
            - {{ .Values.excludeUids | quote }}
            - --exclude-source-namespaces
            - {{ $excludeSourceNamespaces | quote }}
            - --node-ip
            - $(NODE_IP)
            - --resync-period
            - {{ .Values.iptablesSync.resyncPeriod | quote }}
            - --watchdog-period
            - {{ .Values.iptablesSync.watchdogPeriod | quote }}
            - --ipset-maxelem
            - {{ .Values.iptablesSync.ipsetMaxElem | quote }}
            - --ready-file
            - /tmp/ratls-iptables-ready
            - --iptables-metrics-file
            - /tmp/ratls-iptables-metrics.json
            - --log-level
            - {{ .Values.logLevel | quote }}
          env:
            - name: NODE_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          startupProbe:
            exec:
              command:
                - test
                - -f
                - /tmp/ratls-iptables-ready
            periodSeconds: 1
            failureThreshold: 60
          resources:
            {{- toYaml .Values.iptablesSync.resources | nindent 12 }}
      containers:
        - name: ratls-mesh
          image: {{ include "ratls-mesh.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          workingDir: /tmp
          securityContext:
            runAsUser: {{ .Values.uid }}
            runAsGroup: {{ .Values.uid }}
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          args:
            - --platform
            - {{ .Values.platform | quote }}
            - --attestation-service-url
            - {{ $attestationServiceURL | quote }}
            - --outbound-port
            - {{ .Values.ports.outbound | quote }}
            - --inbound-port
            - {{ .Values.ports.inbound | quote }}
            - --health-port
            - {{ .Values.ports.health | quote }}
            - --drain-timeout
            - {{ .Values.drainTimeout | quote }}
            - --iptables-metrics-file
            - /tmp/ratls-iptables-metrics.json
            - --log-level
            - {{ .Values.logLevel | quote }}
            {{- if ne .Values.certTtl "4h" }}
            - --cert-ttl
            - {{ .Values.certTtl | quote }}
            {{- end }}
            {{- if ne .Values.rotationTimeout "30s" }}
            - --rotation-timeout
            - {{ .Values.rotationTimeout | quote }}
            {{- end }}
            {{- if .Values.measurements }}
            - --measurements
            - {{ .Values.measurements | join "," | quote }}
            {{- end }}
            {{- if gt (int .Values.maxConns) 0 }}
            - --max-conns
            - {{ .Values.maxConns | quote }}
            {{- end }}
            {{- if gt (int .Values.maxConnsPerSource) 0 }}
            - --max-conns-per-source
            - {{ .Values.maxConnsPerSource | quote }}
            {{- end }}
            {{- if .Values.idleTimeout }}
            - --idle-timeout
            - {{ .Values.idleTimeout | quote }}
            {{- end }}
            {{- if ne (int .Values.sessionCacheSize) 64 }}
            - --session-cache-size
            - {{ .Values.sessionCacheSize | quote }}
            {{- end }}
            {{- if ne $certMode "self-signed" }}
            - --cert-mode
            - {{ $certMode | quote }}
            - --cds-url
            - {{ $cdsURL | quote }}
            - --ca-poll-interval
            - {{ .Values.cds.caPollInterval | quote }}
            {{- with .Values.global.cdsMeasurements }}
            - --cds-measurements
            - {{ join "," . | quote }}
            {{- end }}
            {{- end }}
          env:
            - name: NODE_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
          ports:
            - name: outbound
              containerPort: {{ .Values.ports.outbound }}
              protocol: TCP
            - name: inbound
              containerPort: {{ .Values.ports.inbound }}
              hostPort: {{ .Values.ports.inbound }}
              protocol: TCP
            - name: health
              containerPort: {{ .Values.ports.health }}
              protocol: TCP
          resources:
            requests:
              {{- toYaml .Values.resources.requests | nindent 14 }}
            limits:
              {{- toYaml .Values.resources.limits | nindent 14 }}
              {{- if .Values.tpm.enabled }}
              {{ .Values.tpm.resource }}: 1
              {{- end }}
          startupProbe:
            httpGet:
              path: /ready
              port: {{ .Values.ports.health }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.ports.health }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          livenessProbe:
            httpGet:
              path: /live
              port: {{ .Values.ports.health }}
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/os
                    operator: In
                    values:
                      - linux
